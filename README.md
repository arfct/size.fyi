# size.fyi

Compare the size of anything — a modern take on [sizeasy](https://web.archive.org/web/2010/http://www.sizeasy.com/).

Hosted on Cloudflare Workers (static assets).

## Development

```sh
npm install
npm run dev      # local dev server
npm run deploy   # deploy to Cloudflare
```
