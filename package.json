{
  "name": "size.fyi",
  "version": "0.0.1",
  "private": true,
  "description": "Compare the size of everyday objects — a modern sizeasy",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.112.0"
  }
}
